channels:
  - conda-forge
dependencies:
  - jupyter-server-proxy~=4.3.0
  # Don't install jupyterhub-singleuser as this should match the version used
  # in the base docker image
  # - jupyterhub-singleuser
  - pip
